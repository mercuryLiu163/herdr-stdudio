feat(v7): Composer 状态栏 / thinking 转圈 / 卡顿根修 / 数据流重排收尾

收尾多会话累积的 V7 数据流重排（F1 用户卡片续行 CJK 拼行根因修复）与 V8 图片附件，
并落地最新三需求：

F5 Composer 状态栏（Double-Bezel 分段胶囊，high-end-visual-design 设计语言）：
- 引用文件 + / 目标下拉 / 视图下拉 / 模型下拉（agent only，发 /model）/ 状态环 / Button-in-Button 发送钮
- 与 V8 composer status 契约并存（v8 spec 零修改）；分段几何断言两两不相交防回归；键盘导航
F6 thinking 转圈（ZCode 式）：working 期 turn 内 thinking 合并为单行实时行（纯 CSS spinner + 思考中 Ns），回合终结定格，仅末回合生效
F7 卡顿根修：spinner 全 CSS keyframes（禁 JS 逐帧）、Elapsed 独立时钟组件（DOM 身份跨流式更新不变）、贴底滚动 rAF、时间 tabular-nums
接手修复：V7 F1 用户卡片 CJK 拼行吞正文；HEAD 既有回归 Esc 中断按钮恢复（D6）；agent 状态 watcher 首见即转换（修轮询间隙 working 永驻）；v5 git 用例陈旧读取竞态
测试：Playwright 83 例（79 passed + 4 fixme skip）0 失败；V1 回归 25/25 ALL GREEN；typecheck 通过
流程：TDD 契约 - 实现 - 评审（REQUEST-CHANGES M1 重叠 + 4 minor 全修复）- judge 3/3 通过