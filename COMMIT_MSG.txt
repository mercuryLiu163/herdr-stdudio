feat(v4): 对话排版规范化 / 斜杠命令面板

F1 对话排版规范化（TUI 转应用排版，不再照搬 terminal）：
- thinking 行（Thought for Ns / Thinking…）解析为可折叠弱化行，提示文案随展开态切换
- meta 扩展（Baked for / Worked for / ... in Ns）归入居中分隔胶囊，testid 迁移为 chat-meta
- TUI 页脚（bypass permissions / shortcuts / 装饰行）整行丢弃，user 判定优先于页脚避免误伤真实 prompt
- 工具输出多列文件列表重排为等宽网格 chip（目录着色，嵌套路径召回，prompt 符号剔除）
- 排版尺度统一：--chat-gap/--chat-indent/--chat-block-x 变量组贯穿全部块类型，左基线一致
F2 斜杠命令面板（Grok bot 风格）：
- agent kind 静态命令表（claude/codex/gemini + 通用兜底）
- 输入 / 弹出、前缀过滤、上下键高亮、Enter/Tab 回填不发送、Esc/blur 关闭、点击选择
- agent.prompt 失败精确回退 pane.send_input（仅 not an active named agent）
测试：Playwright 40 passed / 4 skipped / 0 failed；V1 回归 ALL GREEN；typecheck 通过
流程：TDD 契约 - 实现 - 评审（REQUEST-CHANGES 6 项全修复）- judge 视觉验收 4/4 通过